name: "🧪 Demo Template"
description: "Neutral template to test global propagation across repositories."
title: "[DEMO]: "
labels: ["testing"]
body:
  - type: markdown
    attributes:
      value: |
        ### Global Integration Test
        If this form is visible, the global communication pipeline is working.

  - type: textarea
    id: feedback
    attributes:
      label: "💬 Test Input"
      description: "Type anything here to test the user interface."
      placeholder: "Testing from my local environment..."
    validations:
      required: true
